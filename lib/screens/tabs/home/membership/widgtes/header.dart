import 'package:flutter/material.dart';

class NewMembershipHeader extends StatelessWidget {
  const NewMembershipHeader({super.key});

  @override
  Widget build(BuildContext context) {
    return const Row(
      children: [

        
      ],
    );
  }
}
