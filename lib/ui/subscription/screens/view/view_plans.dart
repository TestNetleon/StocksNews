import 'package:flutter/material.dart';

class ViewAllPlans extends StatelessWidget {
  const ViewAllPlans({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
