import 'package:flutter/material.dart';

class HomeWatchlistContainer extends StatelessWidget {
  const HomeWatchlistContainer({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
