import 'package:flutter/material.dart';
import 'package:stocks_news_new/widgets/linear_bar.dart';

class ToolCompareLinearBar extends StatelessWidget {
  const ToolCompareLinearBar({super.key});

  @override
  Widget build(BuildContext context) {
    return const LinearBarCommon(
      value: 100,
    );
  }
}
